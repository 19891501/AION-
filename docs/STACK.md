# Stack AION

**Maintenant:** Python kernel + gate process isolé (`python -m aion.gate`).
**Rust:** `enforcer_rs/` même contrat JSON — build quand crates.io OK.

Principe: le process qui agit n'est pas le process agent.
